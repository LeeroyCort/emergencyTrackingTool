{% extends 'base.html.twig' %}

{% block title %}Mitglieder bearbeiten{% endblock %}

{% block content %}
<div class="squad-managing">
    <h1>Edit Member</h1>
    {{ form(squadMember.form) }}

    {% if squadMember.selectedMember.assignmentPositions|length == 0 %}
        <a href="{{ path('management_deleteSquadMember', {memberId: squadMember.selectedMember.id}) }}">Löschen</a>
    {% endif %}
    
</div>
{% endblock %}
